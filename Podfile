# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'iReaderApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for iReaderApp
  
  # 私有库
  pod 'IRUIKit', :path => 'Lib/IRUIKit'
  pod 'IRAppearance', :path => 'Lib/IRAppearance'
  
  # 三方库
  pod 'AFNetworking'

  target 'iReaderAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iReaderAppUITests' do
    # Pods for testing
  end

end
